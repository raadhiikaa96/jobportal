@model OnlineJobPortal.Models.UploadImage

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/Home.cshtml";
}

<h2>Details</h2>

<div>
    <h4>UploadImage</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Title)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Title)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.ImagePath)
        </dt>

        <dd>
            <img src="@Url.Content(Model.ImagePath)" width="250" height="250"/>
        </dd>

    </dl>
</div>
<form>
    <div class="form-group">
        <b>Write your review</b>
        <textarea class="form-group" rows="3" name="Comment"></textarea>

    </div>
</form>

<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.UploadImageID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
